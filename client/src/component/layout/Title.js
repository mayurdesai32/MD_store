import React from 'react';

const Title = ({ title }) => {
  return (
    <>
      <div className='title'>{title}</div>
      <div className='titleUndeline' />
    </>
  );
};

export default Title;
